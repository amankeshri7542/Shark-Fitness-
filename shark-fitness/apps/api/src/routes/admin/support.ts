import { Hono } from 'hono';

/**
 * TODO(module): admin/support — tickets, SLA state, assignment, resolution.
 * Mounted by src/app.ts at /v1/admin/support. Add handlers here; do not edit app.ts.
 */
export const supportRoutes = new Hono();
