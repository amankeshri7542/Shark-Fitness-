# Node 24 aborts better-sqlite3 mid-seed with an opaque SIGABRT, so an
# unsupported runtime must fail at install time with a readable message
# rather than during `pnpm db:seed`. Keep in step with ../.node-version.
engine-strict=true
